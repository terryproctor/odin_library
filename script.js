document.getElementsByClassName('empty')[0].addEventListener('click', () =>{
    // show the modal

});

book_form.addEventListener('submit', (e) =>{
    e.preventDefault();
    // create a new book object

    //hide the modal

    //add the book card to the library behind the empty card

});
